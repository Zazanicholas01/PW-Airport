---
title: Transport Websocket
layout: ../../layouts/DocsLayout.astro
description: WebSocket transport responsibilities, message routing, clock sync, and orchestration loops.
---

import DocsIntroHero from '../../components/DocsIntroHero.astro';
import DocsTopicGrid from '../../components/DocsTopicGrid.astro';
import DocsSteps from '../../components/DocsSteps.astro';

export const roles = [
  {
    kicker: 'Server',
    title: 'Connection Lifecycle',
    summary: 'The transport layer owns WebSocket server startup, accepts Unity connections, and manages per-session runtime wiring.',
    tone: 'cyan',
    meta: 'ws_server.py',
  },
  {
    kicker: 'Boundary',
    title: 'Async Message Bus',
    summary: 'Inbound and outbound WebSocket traffic is buffered behind queue-based adapters rather than being handled inline.',
    tone: 'violet',
    meta: 'message_bus.py',
  },
  {
    kicker: 'Dispatch',
    title: 'Setup vs Runtime Routing',
    summary: 'Incoming payloads are routed to setup handling until setup completes, then forwarded into the runtime handling path.',
    tone: 'amber',
    meta: 'incoming_dispatch_loop',
  },
  {
    kicker: 'Clock',
    title: 'Authoritative Time Sync',
    summary: 'Transport publishes clock_sync payloads, applies clock control commands, and bridges backend simulation time to Unity.',
    tone: 'sky',
    meta: 'loops/clock.py',
  },
  {
    kicker: 'Loops',
    title: 'Global Orchestration Tasks',
    summary: 'Transport starts the long-running background tasks that drive scheduling, initial spawns, and periodic synchronization.',
    tone: 'emerald',
    meta: 'loops/*',
  },
  {
    kicker: 'Context',
    title: 'Session and Command Context',
    summary: 'Shared runtime context carries setup/runtime handlers, command builders, session factory access, and loop dependencies.',
    tone: 'rose',
    meta: 'session.py',
  },
];

export const connectionFlow = [
  {
    title: 'Backend starts the WebSocket server',
    body: 'The backend enters transport startup through src.server and initializes the WebSocket server with the application container already assembled.',
  },
  {
    title: 'A Unity connection creates a session transport boundary',
    body: 'For each connection, the runtime creates and starts a WsMessageBus to separate raw WebSocket IO from application-side dispatch.',
  },
  {
    title: 'Session context is assembled',
    body: 'Transport creates a session context carrying setup/runtime handlers, command builders, session access, and the current simulation clock linkage.',
  },
  {
    title: 'Initial transport tasks start',
    body: 'The per-connection dispatcher starts, while transport joins the wider orchestration environment that includes clock and scheduling tasks.',
  },
  {
    title: 'Incoming traffic is consumed from the async bus',
    body: 'Frames are decoded and pushed into queue-backed processing instead of being handled directly inside the WebSocket receive loop.',
  },
];

export const routingFlow = [
  {
    title: 'Messages enter the incoming queue',
    body: 'Raw WebSocket payloads are received by the message bus and exposed as structured incoming items for the dispatcher loop.',
  },
  {
    title: 'Clock control is handled as a transport concern',
    body: 'Clock-related commands are intercepted in the transport layer so simulation-time changes can be applied immediately and propagated back outward.',
  },
  {
    title: 'Setup traffic is routed first',
    body: 'Until setup completion is reached, inbound payloads are forwarded to the setup handler rather than the runtime event path.',
  },
  {
    title: 'Runtime traffic takes over after setup',
    body: 'Once the setup state flips to complete, subsequent inbound events are forwarded into runtime handling instead.',
  },
  {
    title: 'Outbound commands return through the same transport boundary',
    body: 'Commands generated by loops, handlers, or scheduler-driven actions are serialized and written back out through the outgoing bus path.',
  },
];

export const loopFlow = [
  {
    title: 'Clock sync loop publishes authoritative time',
    body: 'The clock loop emits periodic clock_sync payloads so Unity stays aligned with backend simulation time and time-scale changes.',
  },
  {
    title: 'Initial spawn scheduling waits for setup',
    body: 'Transport does not schedule initial operational behavior until setup-derived world state and path data are available.',
  },
  {
    title: 'Flight scheduling runs as a long-lived background loop',
    body: 'The transport layer launches the flight scheduling loop, which repeatedly evaluates the active operational window and emits actionable commands.',
  },
  {
    title: 'Command builders shape the outbound payloads',
    body: 'Transport uses stable command-building helpers to emit payloads such as clock sync and movement-related commands in a consistent format.',
  },
  {
    title: 'Task shutdown is coordinated cleanly',
    body: 'When the server or connection shuts down, transport is responsible for cancelling tasks and stopping the active bus and handler runtime cleanly.',
  },
];

export const keyFiles = [
  {
    kicker: 'Entrypoint',
    title: 'ws_server.py',
    summary: 'Main transport orchestration, server bootstrap, handler wiring, connection handling, and background loop startup.',
  },
  {
    kicker: 'IO Boundary',
    title: 'message_bus.py',
    summary: 'Queue-backed adapter that separates raw WebSocket traffic from application-side message processing.',
  },
  {
    kicker: 'Context',
    title: 'session.py',
    summary: 'Defines the shared session context passed through transport loops and dispatch paths.',
  },
  {
    kicker: 'Clock',
    title: 'loops/clock.py',
    summary: 'Handles clock control and the periodic clock_sync publishing loop.',
  },
  {
    kicker: 'Scheduling',
    title: 'loops/flight_scheduling.py',
    summary: 'Runs the long-lived scheduling loop after setup completion and emits operational commands.',
  },
  {
    kicker: 'Bootstrap',
    title: 'loops/spawn_scheduling.py',
    summary: 'Schedules initial spawn behavior once setup-derived world state is ready.',
  },
];

export const nextReads = [
  {
    href: '/docs/03-end-to-end-workflows/',
    title: 'End-to-End Workflows',
    summary: 'See how transport participates in the broader runtime sequence across setup, scheduling, and feedback.',
    tone: 'amber',
  },
  {
    href: '/docs/09-handlers/',
    title: 'Handlers',
    summary: 'Read the setup and runtime handlers that transport forwards payloads into.',
    tone: 'violet',
  },
  {
    href: '/docs/10-schedulers/',
    title: 'Schedulers',
    summary: 'Go deeper into the scheduling logic that transport executes through its background loops.',
    tone: 'emerald',
  },
  {
    href: '/docs/14-message-contracts/',
    title: 'Message Contracts',
    summary: 'Use the contracts page for payload-level details and vocabulary used over the WebSocket boundary.',
    tone: 'cyan',
  },
];

<DocsIntroHero
  eyebrow="Transport"
  lead="The transport layer is the realtime boundary between Unity and the backend runtime. It owns WebSocket lifecycle, queue-backed message IO, setup-versus-runtime dispatch, clock synchronization, and the orchestration loops that drive scheduling behavior."
  primaryAction={{ href: '/docs/03-end-to-end-workflows/', label: 'Open Workflows' }}
  secondaryAction={{ href: '/docs/14-message-contracts/', label: 'Open Contracts' }}
  meta={['WebSocket server', 'Queue-backed IO', 'Clock sync', 'Scheduling orchestration']}
 />

## Theory Overview

### Transport Runtime Roles

<DocsTopicGrid items={roles} variant="featured" />

### Connection Lifecycle

<DocsSteps items={connectionFlow} variant="timeline" />

### Message Routing

<DocsSteps items={routingFlow} variant="timeline" />

### Background Loops

<DocsSteps items={loopFlow} variant="timeline" />

## Implementation Guide

This section is for direct code navigation when changing transport behavior. The files below are the main implementation entrypoints behind the theory described above.

### Core Transport Scripts

<DocsTopicGrid items={keyFiles} compact />

## Where To Dive Deeper

<DocsTopicGrid items={nextReads} compact />
